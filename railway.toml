[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && npm run build"

[deploy]
startCommand = "cd server && npx prisma generate && npx prisma db push --accept-data-loss && node dist/index.js"
restartPolicyType = "ON_FAILURE"
